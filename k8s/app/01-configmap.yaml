apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: oficina
data:
  NODE_ENV: "production"
  APPLICATION_PORT: "3000"
  JWT_EXPIRES_IN: "1h"
  MAIL_HOST: "smtp.ethereal.email"
  MAIL_PORT: "587"
  MAIL_FROM: '"Oficina SOAT" <noreply@oficina.com>'
